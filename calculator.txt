define sum (int a, int b)
return a+b;
define subtract (int a, int b)
return a-b;
define multiply (int a, int b)
return a*b;
define divide (int a, int b)
return a/b;
define factorial(int n)
{
    int fact =1;
   for(i=1;i<=n;i++)
   {
    fact = fact*i;
   }
   printf(“%d”,fact);
}
define even-odd(int num)
{
 if(num%2 == 0)
  {
    printf(“even number”);
  }
  else
  printf(“odd number”);
}